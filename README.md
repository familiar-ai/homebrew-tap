# Familiar Homebrew tap

Install Guv:

```sh
brew install familiar-ai/tap/guv
brew services start guv
guv up
```

`Formula/guv.rb` is updated by the private `adstastic/v1` Guv release workflow. Release assets contain binary-only Guv artifacts; this tap does not contain `v1` source.
